cat > ecosystem.config.cjs << 'EOL'
module.exports = {
	apps: [
		{
			name: 'multiaudioplayer',
			script: './dist/main.js',
			instances: 'max',
			exec_mode: 'cluster',
			autorestart: true,
			watch: false,
			max_memory_restart: '200M',
			env: {
				NODE_ENV: 'development',
			},
			env_production: {
				NODE_ENV: 'production',
				SECRET: 'MYAPP',
				STATIC_DIRECTORY_PATH: 'static',
				PORT: 8000,
				HOST: 'multiaudioplayer.ru',
			},
		},
	],
};
EOL

PORT=8000
SECRET='MYAPP'
STATIC_DIRECTORY_PATH=static
HOST="multiaudioplayer.ru"
DATABASE_URL =
	'postgresql://ig_kolmakov:(|GN9OdHVVjb:N@49e69a2bdb066eade1da855e.twc1.net:5432/multi_audio_player'

    sudo ln -s /etc/nginx/sites-available/multiaudioplayer.ru /etc/nginx/sites-enabled/

    sudo nano /etc/nginx/sites-available/multiaudioplayer

    sudo mkdir /var/www/store && cp -r ~/multi-audio-player/dist/* /var/www/store/

    sudo ln -s /etc/nginx/sites-available/multiaudioplayer.ru /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

server {
    listen 80;
    server_name multiaudioplayer.ru www.multiaudioplayer.ru;
    root /var/www/store;
    index index.html;

    location / {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo certbot --nginx -d multiaudioplayer.ru -d www.multiaudioplayer.ru

sudo nano /etc/nginx/sites-available/multiaudioplayer.ru
sudo nano /etc/nginx/sites-available/multiaudioplayer